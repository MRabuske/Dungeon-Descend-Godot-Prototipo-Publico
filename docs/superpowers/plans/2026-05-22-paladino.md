# Paladino (Paladin) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar o Paladino como sétima classe jogável, com skills SkillSmite e SkillCuraMaos, e mecânica de Smite Divino (buff de dano MP-based no próximo ataque).

**Architecture:** Criar 3 novos arquivos (skill_smite.gd, skill_cura_maos.gd, paladino.gd), registrar em BattleState (ALL_HERO_DATA / TURN_QUEUE / PLAYERS), adicionar apply_smite_status() + smite em _apply_attack() + badge no status panel, e wiring do handler em battle_scene.gd. Verificação por inspeção de código — sem editor Godot ativo.

**Tech Stack:** GDScript 4.6, Godot 4.6. Sem editor ativo — verificação por inspeção de código.

---

## Arquivos

| Ação | Arquivo |
|---|---|
| Criar | `actions/skills/skill_smite.gd` |
| Criar | `actions/skills/skill_cura_maos.gd` |
| Criar | `heroes/paladino.gd` |
| Modificar | `battle/battle_state.gd` |
| Modificar | `battle/battle_scene.gd` |
| Modificar | `battle/ui/status_panel.gd` |
| Modificar | `tests/run_tests.gd` |

---

### Task 1: Criar SkillSmite e SkillCuraMaos

**Files:**
- Create: `actions/skills/skill_smite.gd`
- Create: `actions/skills/skill_cura_maos.gd`

Sem testes automatizados — verificação por inspeção de código.

- [ ] **Step 1: Criar `actions/skills/skill_smite.gd`**

```gdscript
class_name SkillSmite
extends ActionData

func _init() -> void:
	label       = "Smite Divino"
	action_type = Type.END_TURN
	shape       = SHAPE_HEXAGON
	color_idx   = COLOR_SPELL
	self_target = true
	mp_cost     = 15
```

- [ ] **Step 2: Criar `actions/skills/skill_cura_maos.gd`**

```gdscript
class_name SkillCuraMaos
extends ActionData

func _init() -> void:
	label            = "Cura das Mãos"
	action_type      = Type.ATTACK
	shape            = SHAPE_HEXAGON
	color_idx        = COLOR_SPELL
	attack_range     = 1
	proj_color       = Color(0.30, 1.00, 0.50)
	targets_allies   = true
	self_target      = true
	damage_attribute = DamageAttribute.WIS
	base_damage_min  = 15
	base_damage_max  = 25
	mp_cost          = 20
```

- [ ] **Step 3: Verificar a lógica (inspeção de código)**

- `SkillSmite.action_type = Type.END_TURN` → vai para o branch END_TURN em `_execute_current_item()` ✅
- `SkillSmite.mp_cost = 15` → `is_item_available()` bloqueia se MP < 15 ✅
- `SkillCuraMaos.action_type = Type.ATTACK` → entra no branch ATTACK genérico em `_execute_current_item()` sem handler especial ✅
- `SkillCuraMaos.targets_allies = true` → `_apply_attack()` vai pelo branch de heal ✅
- `SkillCuraMaos.damage_attribute = WIS` → dano usa `_get_attr_modifier(WIS)` ✅
- `SkillCuraMaos.mp_cost = 20` → `is_item_available()` bloqueia se MP < 20 ✅

- [ ] **Step 4: Commit e push**

```
git add actions/skills/skill_smite.gd actions/skills/skill_cura_maos.gd
git commit -m "feat: add SkillSmite and SkillCuraMaos action files"
git push
```

---

### Task 2: Criar PaladinoData e registrar em BattleState

**Files:**
- Create: `heroes/paladino.gd`
- Modify: `battle/battle_state.gd`

Sem testes automatizados — verificação por inspeção de código.

- [ ] **Step 1: Criar `heroes/paladino.gd`**

```gdscript
class_name PaladinoData
extends HeroData

func _init() -> void:
	hero_name    = "Paladino"
	hero_class   = "Paladin"
	level        = 4
	base_hp      = 80
	max_hp       = 110
	base_mp      = 40
	max_mp       = 60
	ac           = 16
	initiative   = 6
	speed        = 6
	proficiency  = 3
	strength     = 16
	dexterity    = 10
	intelligence = 8
	wisdom       = 14
	constitution = 15
	damage_reduction = 0
	actions = [AtqDivino.new(), AcaoMover.new()]
	skills  = [SkillSmite.new(), SkillCuraMaos.new()]
```

- [ ] **Step 2: Adicionar Paladino a `ALL_HERO_DATA` em `battle/battle_state.gd`**

Leia `battle/battle_state.gd`. Localizar (linhas 15-22):

```gdscript
static var ALL_HERO_DATA: Dictionary = {
	"Guerreiro": GuerreiroData.new(),
	"Mago":      MagoData.new(),
	"Arqueiro":  ArqueiroData.new(),
	"Clérigo":   ClérigoData.new(),
	"Ladrao":    LadraoData.new(),
	"Bárbaro":   BarbaroData.new(),
}
```

Substituir por:

```gdscript
static var ALL_HERO_DATA: Dictionary = {
	"Guerreiro": GuerreiroData.new(),
	"Mago":      MagoData.new(),
	"Arqueiro":  ArqueiroData.new(),
	"Clérigo":   ClérigoData.new(),
	"Ladrao":    LadraoData.new(),
	"Bárbaro":   BarbaroData.new(),
	"Paladino":  PaladinoData.new(),
}
```

- [ ] **Step 3: Adicionar Paladino a `TURN_QUEUE` em `battle/battle_state.gd`**

Localizar (linhas 31-42):

```gdscript
static var TURN_QUEUE: Array = [
	{"name": "Guerreiro",        "is_player": true},
	{"name": "Goblin Scout",     "is_player": false, "type": "Goblin",  "ac": 13},
	{"name": "Mago",             "is_player": true},
	{"name": "Arqueiro",         "is_player": true},
	{"name": "Orc Warrior",      "is_player": false, "type": "Orc",     "ac": 15},
	{"name": "Clérigo",          "is_player": true},
	{"name": "Dark Mage",        "is_player": false, "type": "Mage",    "ac": 11},
	{"name": "Skeleton Archer",  "is_player": false, "type": "Undead",  "ac": 12},
	{"name": "Ladrao",           "is_player": true},
	{"name": "Bárbaro",          "is_player": true},
]
```

Substituir por:

```gdscript
static var TURN_QUEUE: Array = [
	{"name": "Guerreiro",        "is_player": true},
	{"name": "Goblin Scout",     "is_player": false, "type": "Goblin",  "ac": 13},
	{"name": "Mago",             "is_player": true},
	{"name": "Arqueiro",         "is_player": true},
	{"name": "Orc Warrior",      "is_player": false, "type": "Orc",     "ac": 15},
	{"name": "Clérigo",          "is_player": true},
	{"name": "Dark Mage",        "is_player": false, "type": "Mage",    "ac": 11},
	{"name": "Skeleton Archer",  "is_player": false, "type": "Undead",  "ac": 12},
	{"name": "Ladrao",           "is_player": true},
	{"name": "Bárbaro",          "is_player": true},
	{"name": "Paladino",         "is_player": true},
]
```

- [ ] **Step 4: Adicionar Paladino a `PLAYERS` em `battle/battle_state.gd`**

Localizar (linhas 44-57):

```gdscript
static var PLAYERS: Array = [
	{"name": "Guerreiro", "hp": 80,  "max_hp": 100, "mp": 40,  "max_mp": 80,
	 "class": "Fighter", "level": 4, "ac": 16, "initiative": 8,  "speed": 7, "proficiency": 3},
	{"name": "Mago",      "hp": 60,  "max_hp": 100, "mp": 100, "max_mp": 100,
	 "class": "Wizard",  "level": 4, "ac": 12, "initiative": 5,  "speed": 6, "proficiency": 3},
	{"name": "Arqueiro",  "hp": 70,  "max_hp": 100, "mp": 30,  "max_mp": 60,
	 "class": "Ranger",  "level": 4, "ac": 14, "initiative": 10, "speed": 8, "proficiency": 3},
	{"name": "Clérigo",   "hp": 75,  "max_hp": 100, "mp": 90,  "max_mp": 100,
	 "class": "Cleric",  "level": 4, "ac": 15, "initiative": 6,  "speed": 7, "proficiency": 3},
	{"name": "Ladrao",    "hp": 55,  "max_hp": 100, "mp": 20,  "max_mp": 40,
	 "class": "Rogue",   "level": 4, "ac": 13, "initiative": 9,  "speed": 9, "proficiency": 3},
	{"name": "Bárbaro",  "hp": 95,  "max_hp": 120, "mp": 0,   "max_mp": 0,
	 "class": "Barbarian", "level": 4, "ac": 12, "initiative": 7, "speed": 6, "proficiency": 3},
]
```

Substituir por:

```gdscript
static var PLAYERS: Array = [
	{"name": "Guerreiro", "hp": 80,  "max_hp": 100, "mp": 40,  "max_mp": 80,
	 "class": "Fighter", "level": 4, "ac": 16, "initiative": 8,  "speed": 7, "proficiency": 3},
	{"name": "Mago",      "hp": 60,  "max_hp": 100, "mp": 100, "max_mp": 100,
	 "class": "Wizard",  "level": 4, "ac": 12, "initiative": 5,  "speed": 6, "proficiency": 3},
	{"name": "Arqueiro",  "hp": 70,  "max_hp": 100, "mp": 30,  "max_mp": 60,
	 "class": "Ranger",  "level": 4, "ac": 14, "initiative": 10, "speed": 8, "proficiency": 3},
	{"name": "Clérigo",   "hp": 75,  "max_hp": 100, "mp": 90,  "max_mp": 100,
	 "class": "Cleric",  "level": 4, "ac": 15, "initiative": 6,  "speed": 7, "proficiency": 3},
	{"name": "Ladrao",    "hp": 55,  "max_hp": 100, "mp": 20,  "max_mp": 40,
	 "class": "Rogue",   "level": 4, "ac": 13, "initiative": 9,  "speed": 9, "proficiency": 3},
	{"name": "Bárbaro",  "hp": 95,  "max_hp": 120, "mp": 0,   "max_mp": 0,
	 "class": "Barbarian", "level": 4, "ac": 12, "initiative": 7, "speed": 6, "proficiency": 3},
	{"name": "Paladino",  "hp": 80,  "max_hp": 110, "mp": 40,  "max_mp": 60,
	 "class": "Paladin", "level": 4, "ac": 16, "initiative": 6,  "speed": 6, "proficiency": 3},
]
```

- [ ] **Step 5: Verificar a lógica (inspeção de código)**

- `ALL_HERO_DATA["Paladino"]` → `PaladinoData.new()` → disponível para `_load_hero_actions()` ✅
- `TURN_QUEUE[10]` → `{"name": "Paladino", "is_player": true}` → `combatant_positions` e `combatant_statuses` ganham índice 10 em `_init()` ✅
- `PLAYERS[6]` → `{"name": "Paladino", ...}` → `setup_party()` usa `to_combat_dict()` então a entry estática é apenas para o cenário de teste ✅
- `PaladinoData.actions = [AtqDivino, AcaoMover]` → `_load_hero_actions()` duplica corretamente ✅

- [ ] **Step 6: Commit e push**

```
git add heroes/paladino.gd battle/battle_state.gd
git commit -m "feat: add PaladinoData and register in BattleState"
git push
```

---

### Task 3: Mecânica de Smite Divino — apply_smite_status, _apply_attack, status text, badge

**Files:**
- Modify: `battle/battle_state.gd`
- Modify: `battle/ui/status_panel.gd`

Sem testes automatizados — verificação por inspeção de código.

- [ ] **Step 1: Adicionar `apply_smite_status()` em `battle/battle_state.gd`**

Leia `battle/battle_state.gd`. Após a função `apply_fury_status()` (que termina por volta da linha 760), adicionar:

```gdscript
func apply_smite_status(player_idx: int) -> void:
	if player_idx < 0 or player_idx >= PLAYERS.size():
		return
	var pname: String = PLAYERS[player_idx]["name"]
	for i in range(TURN_QUEUE.size()):
		if TURN_QUEUE[i]["is_player"] and TURN_QUEUE[i]["name"] == pname:
			combatant_statuses[i]["smite"] = 1
			_log("Paladino ativa Smite Divino!", "status")
			break
```

- [ ] **Step 2: Adicionar bloco de smite em `_apply_attack()` em `battle/battle_state.gd`**

Localizar em `_apply_attack()` o bloco de fury (por volta da linha 1041):

```gdscript
		if combatant_statuses[active_index].get("fury", 0) > 0:
			damage += 4
```

Após essa linha, adicionar:

```gdscript
		if combatant_statuses[active_index].get("smite", 0) > 0:
			var smite_bonus: int = randi_range(8, 16)
			combatant_statuses[active_index].erase("smite")
			_log("%s aplica Smite Divino! +%d dano" % [get_active_combatant()["name"], smite_bonus], "status")
			damage += smite_bonus
```

- [ ] **Step 3: Adicionar smite em `get_active_status_text()` em `battle/battle_state.gd`**

Localizar em `get_active_status_text()` (por volta da linha 1106):

```gdscript
	if status.get("fury",      0) > 0: parts.append("Fúria +4 dano")
	return "  ".join(parts)
```

Substituir por:

```gdscript
	if status.get("fury",      0) > 0: parts.append("Fúria +4 dano")
	if status.get("smite",     0) > 0: parts.append("Smite +8-16 dano")
	return "  ".join(parts)
```

- [ ] **Step 4: Adicionar badge "smite" em `battle/ui/status_panel.gd`**

Leia `battle/ui/status_panel.gd`. Localizar o `badge_map` em `_refresh_status_badges()` (por volta da linha 276):

```gdscript
		var badge_map := [
			["defending", "Defender +2 AC",  Color(0.3, 0.6, 1.0)],
			["stun",      "Atordoado",        Color(1.0, 0.9, 0.2)],
			["stunned",   "Atordoado",        Color(1.0, 0.9, 0.2)],
			["raging",    "Furia +3 dano",    Color(1.0, 0.3, 0.2)],
			["aiming",    "Mirando +2 alc",   Color(0.6, 1.0, 0.6)],
			["furtivo",   "Furtivo",          Color(0.7, 0.4, 1.0)],
			["fury",      "Fúria",            Color(1.0, 0.2, 0.0)],
		]
```

Substituir por:

```gdscript
		var badge_map := [
			["defending", "Defender +2 AC",  Color(0.3, 0.6, 1.0)],
			["stun",      "Atordoado",        Color(1.0, 0.9, 0.2)],
			["stunned",   "Atordoado",        Color(1.0, 0.9, 0.2)],
			["raging",    "Furia +3 dano",    Color(1.0, 0.3, 0.2)],
			["aiming",    "Mirando +2 alc",   Color(0.6, 1.0, 0.6)],
			["furtivo",   "Furtivo",          Color(0.7, 0.4, 1.0)],
			["fury",      "Fúria",            Color(1.0, 0.2, 0.0)],
			["smite",     "Smite",            Color(1.0, 0.9, 0.2)],
		]
```

- [ ] **Step 5: Verificar a lógica (inspeção de código)**

- `apply_smite_status(pidx)` busca TURN_QUEUE pelo nome e define `combatant_statuses[i]["smite"] = 1` ✅
- Smite em `_apply_attack()`: lido após fury, consome com `erase("smite")` → aplica apenas uma vez ✅
- `randi_range(8, 16)` → bonus aleatório 8-16 ✅
- `get_active_status_text()` inclui "Smite +8-16 dano" quando smite>0 ✅
- Badge "smite" com cor dourada `Color(1.0, 0.9, 0.2)` aparece no painel de status ✅

- [ ] **Step 6: Commit e push**

```
git add battle/battle_state.gd battle/ui/status_panel.gd
git commit -m "feat: add Smite Divino mechanics — apply_smite_status, damage bonus, status text, badge"
git push
```

---

### Task 4: Wiring do SkillSmite em battle_scene.gd

**Files:**
- Modify: `battle/battle_scene.gd`

Sem testes automatizados — verificação por inspeção de código.

- [ ] **Step 1: Adicionar handler `elif item is SkillSmite` em `_execute_current_item()`**

Leia `battle/battle_scene.gd`. Localizar o branch END_TURN em `_execute_current_item()` (por volta da linha 204):

```gdscript
			ActionData.Type.END_TURN:
				if item is AcaoDefender:
					_state.apply_defender_action(_state.get_active_player_index())
				elif item is AcaoEsperar:
					var pidx: int = _state.get_active_player_index()
					if pidx >= 0 and BattleState.PLAYERS[pidx]["class"] == "Rogue":
						_state.apply_furtivo_status(pidx)
				elif item is SkillSegundoVento:
					var pidx: int = _state.get_active_player_index()
					if pidx >= 0 and item.pp > 0:
						item.pp -= 1
						var p: Dictionary = BattleState.PLAYERS[pidx]
						p["hp"] = mini(p["hp"] + 15, p["max_hp"])
						_state._log("%s usa Segundo Vento! +15 HP" % BattleState.PLAYERS[pidx]["name"], "status")
				_state.advance_turn()
				_after_advance()
```

Substituir por:

```gdscript
			ActionData.Type.END_TURN:
				if item is AcaoDefender:
					_state.apply_defender_action(_state.get_active_player_index())
				elif item is AcaoEsperar:
					var pidx: int = _state.get_active_player_index()
					if pidx >= 0 and BattleState.PLAYERS[pidx]["class"] == "Rogue":
						_state.apply_furtivo_status(pidx)
				elif item is SkillSegundoVento:
					var pidx: int = _state.get_active_player_index()
					if pidx >= 0 and item.pp > 0:
						item.pp -= 1
						var p: Dictionary = BattleState.PLAYERS[pidx]
						p["hp"] = mini(p["hp"] + 15, p["max_hp"])
						_state._log("%s usa Segundo Vento! +15 HP" % BattleState.PLAYERS[pidx]["name"], "status")
				elif item is SkillSmite:
					var pidx: int = _state.get_active_player_index()
					if pidx >= 0:
						var p: Dictionary = BattleState.PLAYERS[pidx]
						if p["mp"] >= item.mp_cost:
							p["mp"] -= item.mp_cost
							_state.apply_smite_status(pidx)
						else:
							_state._log("MP insuficiente para Smite!", "status")
				_state.advance_turn()
				_after_advance()
```

**Atenção:** `_state.advance_turn()` fica FORA do bloco `elif item is SkillSmite:`, no nível do `ActionData.Type.END_TURN:`. É chamado uma vez após toda a chain if-elif, como para os outros handlers.

- [ ] **Step 2: Verificar a lógica (inspeção de código)**

- `elif item is SkillSmite:` está DENTRO do `ActionData.Type.END_TURN:` branch ✅
- MP é verificado antes de chamar `apply_smite_status()` ✅
- `_state.advance_turn()` e `_after_advance()` são chamados apenas UMA vez, fora da chain de elif ✅
- Se o MP for insuficiente, loga a mensagem mas ainda chama `advance_turn()` (turno avança) ✅

- [ ] **Step 3: Commit e push**

```
git add battle/battle_scene.gd
git commit -m "feat: wire SkillSmite handler in battle_scene.gd END_TURN branch"
git push
```

---

### Task 5: Testes para Paladino

**Files:**
- Modify: `tests/run_tests.gd`

Sem testes automatizados — verificação por inspeção de código.

- [ ] **Step 1: Adicionar testes de Paladino em `tests/run_tests.gd`**

Leia `tests/run_tests.gd`. Localizar o final do arquivo (após os últimos testes de Bárbaro/per-hero-actions, por volta da linha 529). Adicionar antes do fechamento da função `_run_all()`:

```gdscript
	# ── Paladino tests ──────────────────────────────────────────────────────────

	# Task 1: SkillSmite e SkillCuraMaos
	var sm := SkillSmite.new()
	_eq("SkillSmite label", sm.label, "Smite Divino")
	_eq("SkillSmite mp_cost", sm.mp_cost, 15)
	_eq("SkillSmite action_type is END_TURN", sm.action_type, ActionData.Type.END_TURN)
	_eq("SkillSmite self_target", sm.self_target, true)

	var cm := SkillCuraMaos.new()
	_eq("SkillCuraMaos label", cm.label, "Cura das Mãos")
	_eq("SkillCuraMaos mp_cost", cm.mp_cost, 20)
	_eq("SkillCuraMaos targets_allies", cm.targets_allies, true)
	_eq("SkillCuraMaos damage_attribute is WIS", cm.damage_attribute, ActionData.DamageAttribute.WIS)
	_eq("SkillCuraMaos base_damage_min", cm.base_damage_min, 15)
	_eq("SkillCuraMaos base_damage_max", cm.base_damage_max, 25)

	# Task 2: PaladinoData stats e actions
	var pal_data: HeroData = BattleState.ALL_HERO_DATA.get("Paladino", null)
	_true("ALL_HERO_DATA contains Paladino", pal_data != null)
	_eq("Paladino strength is 16",    pal_data.strength,   16)
	_eq("Paladino wisdom is 14",      pal_data.wisdom,     14)
	_eq("Paladino max_hp is 110",     pal_data.max_hp,     110)
	_eq("Paladino base_mp is 40",     pal_data.base_mp,    40)
	_eq("Paladino class is Paladin",  pal_data.hero_class, "Paladin")
	_eq("Paladino ac is 16",          pal_data.ac,         16)
	_eq("Paladino speed is 6",        pal_data.speed,      6)
	_true("Paladino actions[0] is AtqDivino",  pal_data.actions.size() >= 1 and pal_data.actions[0] is AtqDivino)
	_true("Paladino actions[1] is AcaoMover",  pal_data.actions.size() >= 2 and pal_data.actions[1] is AcaoMover)
	_true("Paladino skills[0] is SkillSmite",  pal_data.skills.size() >= 1 and pal_data.skills[0] is SkillSmite)
	_true("Paladino skills[1] is SkillCuraMaos", pal_data.skills.size() >= 2 and pal_data.skills[1] is SkillCuraMaos)

	# Task 3: apply_smite_status
	var s_smite := BattleState.new()
	s_smite.apply_smite_status(0)  # Guerreiro (PLAYERS[0]) → TURN_QUEUE[0]
	_true("apply_smite_status sets smite on queue idx 0",
		  s_smite.combatant_statuses[0].get("smite", 0) > 0)

	# Task 3: smite em _apply_attack — dano aumentado e status consumido
	var s_pal := BattleState.new()
	s_pal.active_index = 0
	s_pal.combatant_statuses[0]["smite"] = 1
	s_pal.enemy_hp["Goblin Scout"] = 200
	for i in range(BattleState.TURN_QUEUE.size()):
		s_pal.combatant_positions[i] = Vector2i(i, 0)
	s_pal._apply_attack(1)  # ataca Goblin Scout (TURN_QUEUE[1])
	_true("smite adds damage (minimum base_min + prof + 8 = 15)",
		  s_pal.last_attack_info.get("amount", 0) >= 15)
	_true("smite is consumed after attack",
		  s_pal.combatant_statuses[0].get("smite", 0) == 0)

	# Task 3: is_item_available bloqueia SkillSmite quando MP insuficiente
	var s_mp := BattleState.new()
	BattleState.PLAYERS[0]["mp"] = 10  # MP < 15
	var sm2 := SkillSmite.new()
	_true("SkillSmite unavailable when MP < 15",
		  not s_mp.is_item_available(sm2))
	BattleState.PLAYERS[0]["mp"] = 40  # restaurar
```

- [ ] **Step 2: Verificar a lógica (inspeção de código)**

- Testes de SkillSmite e SkillCuraMaos verificam label, mp_cost, action_type, targets_allies, damage_attribute ✅
- Testes de PaladinoData verificam stats principais e actions/skills arrays ✅
- `apply_smite_status(0)` usa PLAYERS[0] (Guerreiro) para verificar o mecanismo de busca por nome ✅
- `_apply_attack` com `smite=1`: minimum damage = `base_dmg_min(4) + prof(3) + smite_min(8) = 15` ✅
- Smite é consumido (erase) após `_apply_attack()` ✅
- `is_item_available` retorna false quando MP < mp_cost ✅
- MP é restaurado para 40 após o teste para não afetar testes subsequentes ✅

- [ ] **Step 3: Commit e push**

```
git add tests/run_tests.gd
git commit -m "test: add Paladino unit tests — stats, skills, smite status"
git push
```
